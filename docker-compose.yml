version: '3.4'
services:
  mongo:
    image: mongo
    volumes:
      - ./mongo-data:/data/db

  server:
    build:
      context: ./server
      target: production
    depends_on:
      - mongo
    environment:
      - PORT=${PORT}
      - MONGO_URI=${MONGO_URI}
    ports:
      - ${PORT}:${PORT}
